 <script type="text/x-handlebars">
    <h2>Welcome to Ember.js</h2>

    {{outlet}}
  </script>

  <script type="text/x-handlebars" id="index">
    <ul>
    {{#each item in model}}
      <li>
        <div>
          {{item.user.displayName}}
          <a {{bind-attr href=item.issue.self}}>here {{item.fields.summary}}</a>
        </div>
      </li>
    {{/each}}
    </ul>
  </script>